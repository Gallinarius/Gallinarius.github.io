# Sonnen Dashboard — Tesla Fleet API

This repository hosts the public key required for the Sonnen Dashboard app
to connect to Tesla Fleet API.

The key file at `.well-known/appspecific/com.tesla.3p.public-key.pem` is
a standard EC public key used for partner verification with Tesla.

The corresponding private key is stored locally on the owner's machine only.
